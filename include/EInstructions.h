#ifndef EINSTRUCTIONS_H
#define EINSTRUCTIONS_H

// instructions for ConSALTing Machine: set new salt concentration,
// reset salt amount

enum EInstructions { SET_SALT_CONCENTRATION, RESET_SALT_AMOUNT};

#endif